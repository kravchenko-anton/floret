---
name: transcript
description: Fetch a YouTube transcript as a reflowed script with Floret.
---

# Transcript

Use Floret MCP tool `get_transcript` with the YouTube URL or video ID.

Optional `lang` (ISO 639-1, e.g. `en`). Return the script text; do not invent dialogue.
